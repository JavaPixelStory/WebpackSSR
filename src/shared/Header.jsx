import * as React from 'react';
import style from './header.scss'

export function Header() {
    return(
        <header>
            <h1 className={style.exam}>Ready!</h1>
        </header>
    );
}